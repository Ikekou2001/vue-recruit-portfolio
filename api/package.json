{
  "name": "api",
  "version": "1.0.0",
  "description": "Azure Functions API for Vue Recruit Portfolio",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "pnpm run build",
    "start": "func start",
    "dev": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/functions": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.x",
    "typescript": "^5.0.0",
    "azure-functions-core-tools": "^4"
  }
}
